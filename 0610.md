# 融合页操作指南

## 一、数据准备

融合页需要一个**标定数据根目录**，该目录下包含若干 `calib_XXX_<timestamp>/` 子目录，每个子目录代表一次标定拍摄。

### 1.1 每个 calib 子目录内必须包含

| 文件 | 说明 |
|---|---|
| `pose.json` | 位姿估计结果（rvec / tvec / 内参 / depth_scale） |
| `rgb.png` | 彩色图像 |
| `depth_aligned.npy` | 对齐后的深度图（uint16 NumPy 数组） |

### 1.2 可选：Mask 图像

如果需要在融合点云中高亮特定物体，可在子目录中放置 mask 图像：

```
calib_001_20250610_143000/
├── pose.json
├── rgb.png
├── depth_aligned.npy
├── cup_mask.png        # 杯子的掩码
└── book_mask.png       # 书的掩码
```

**Mask 命名规则：**

- 格式：`<对象名>_mask.png` 或 `<对象名>_mask.jpg`
- 例如 `cup_mask.png` → 对象名为 `cup`
- 与 `rgb.png` **同分辨率**（如 1280×720）
- **二值图像**：白色区域（像素值 > 127）= 目标物体，黑色 = 背景
- 每个目录可以有**多个 mask**，不同对象会自动分配不同高亮颜色

### 1.3 目录结构示例

```
my_calib_data/                          # ← 融合页选择这个目录
├── calib_001_20250610_142500/
│   ├── pose.json
│   ├── rgb.png
│   ├── depth_aligned.npy
│   ├── depth_aligned.png              # (可选) 深度可视化图
│   ├── rgb_detection.png              # (可选) 检测叠加图
│   └── cup_mask.png                   # (可选) mask
├── calib_002_20250610_142515/
│   ├── pose.json
│   ├── rgb.png
│   ├── depth_aligned.npy
│   └── cup_mask.png
├── calib_003_20250610_142530/
│   ├── pose.json
│   ├── rgb.png
│   └── depth_aligned.npy
└── calib_004_20250610_142545/
    ├── pose.json
    ├── rgb.png
    └── depth_aligned.npy
```

> calib 子目录由标定页的「💾 保存全部」功能自动生成。Mask 图像需要自行准备（如用标注工具或 SAM 等模型生成），放到对应目录即可。

---

## 二、操作流程

### 步骤 1：获取标定数据

在「标定」页完成多角度 Charuco 板拍摄（建议 15–30 张），点击「💾 保存全部」导出到一个文件夹。

### 步骤 2：准备 Mask（可选）

如果需要高亮特定物体，在需要 mask 的 `calib_*` 子目录中放入 `对象名_mask.png`。不需要每帧都有 mask——缺少 mask 的帧中该物体不会高亮。

### 步骤 3：切换到融合页

点击顶部的「融合」标签页。

### 步骤 4：加载数据

1. 点击「浏览...」选择包含 `calib_*` 子目录的根文件夹
2. 点击「加载」

系统会扫描所有子目录，仅加载同时包含 `pose.json` + `rgb.png` + `depth_aligned.npy` 的有效采集。加载后列表会显示每帧的角点数和重投影误差。

### 步骤 5：选择采集

- 列表每行有复选框，默认**全选**
- 可用「全选 / 取消全选」批量操作
- 通常保留全选即可

### 步骤 6：调整参数

| 参数 | 说明 | 建议值 |
|---|---|---|
| 体素下采样 (m) | 值越大点越稀疏，越小越密集 | 0.003 |
| 最远距离 (m) | 超过此距离的深度点被丢弃 | 2.0 |
| 像素跳采 | 每隔 N 像素取一点，值越大越快 | 4 |
| **启用 Mask 高亮** | 勾选后 mask 区域用高亮色显示 | 按需 |

### 步骤 7：融合并显示

点击「🔬 融合并显示」。系统会：

1. 将每帧的深度图反投影为相机坐标系点云
2. 利用位姿将点云变换到统一的标定板坐标系
3. 合并所有帧的点云
4. 如果启用了 mask，对 mask 区域替换为高亮颜色
5. 体素下采样去重
6. 打开 Open3D 窗口显示结果

### 步骤 8：保存

点击「💾 保存 PLY」将融合点云导出为 PLY 文件，可在 MeshLab、CloudCompare 等工具中打开。

---

## 三、Mask 高亮颜色

启用 mask 后，不同对象按发现顺序分配不同颜色（最多 8 种循环）：

| 顺序 | 对象示例 | 颜色 |
|---|---|---|
| 第 1 个 | cup | 亮红 |
| 第 2 个 | book | 亮绿 |
| 第 3 个 | box | 亮黄 |
| 第 4 个 | pen | 亮青 |
| 第 5 个 | ... | 亮品红 |

**注意：** 多个 mask 有重叠区域时，按对象名字母序优先匹配（`book` 优先于 `cup`）。

---

## 四、常见问题

### Q: 加载后列表为空

- 确认选择的是包含 `calib_*` 子目录的**根文件夹**，而不是某个 `calib_*` 子目录
- 确认每个子目录包含 `pose.json`、`rgb.png`、`depth_aligned.npy`
- 确认 `pose.json` 中 `success` 字段为 `true`

### Q: 融合后点云为空

- 确认深度图中有有效数据（不是全黑/全零）
- 尝试增大「最远距离」或减小「像素跳采」
- 检查标定时标定板是否在相机有效范围内

### Q: Mask 没有生效

- 确认勾选了「启用 Mask 高亮」
- 确认 mask 文件名格式为 `对象名_mask.png`（注意下划线）
- 确认 mask 图像分辨率与 `rgb.png` 一致
- 查看日志区是否有 mask 相关的警告信息

### Q: 融合后点云位置错乱

- 确认所有帧的 Charuco 板检测都是「成功」状态
- 删除重投影误差过大的帧（列表中有显示）
- 确保拍摄时标定板没有移动
